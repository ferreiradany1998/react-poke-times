import React from 'react'

const Contact = (props) => {

    //Programmatic redirect
    //Components exist thanks to <Route>
    //setTimeout(() => {
    //    props.history.push('/about')
    //}, 2000);

    return (
        <div className="container">
            <h4 className="center">Contact</h4>
            <p>lorem</p>
        </div>
    )
}

export default Contact